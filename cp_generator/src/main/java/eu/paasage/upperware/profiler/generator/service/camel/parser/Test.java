package eu.paasage.upperware.profiler.generator.service.camel.parser;

import eu.paasage.upperware.profiler.generator.service.camel.parser.impl.ExpressionServiceImpl;
import org.mariuszgromada.math.mxparser.Expression;
import org.mariuszgromada.math.mxparser.mXparser;
import org.mariuszgromada.math.mxparser.parsertokens.Token;

import java.util.List;

public class Test {
//    public static void main(String[] args) {
//
//        String formula = "0.5 * (-4.5 * (AppC + RT_AVG/AppD/100)^(2 + 3 * AppD) * (AppC * RT_AVG - AppD/100)) + 0.5 * ((AppC - AppActPrice + Xyz)/(AppD*AppPrice))";
//
//
//        Expression expression = new Expression(formula);
//
//        List<Token> copyOfInitialTokens = expression.getCopyOfInitialTokens();
//
//
//        mXparser.consolePrintTokens(copyOfInitialTokens);
//
////
////        ExpressionServiceImpl expressionService = new ExpressionServiceImpl(null, null, null, null);
////        expressionService.parse("myNameIsFormula", formula, null, camelModel);
//    }


}
